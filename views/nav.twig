<nav class="navbar navbar-light bg-light">
    <div>
        <h1>{{ title }}</h1>
    </div>

    <div class="flex">
            <p>{{ username }}</p>
            <img src="{{ avatarUrl }}" width="50">
    </div>
</nav>
